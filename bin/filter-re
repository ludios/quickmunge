#!/usr/bin/python -B

"""
Print out lines that match a Python regexp.
"""

import sys
import re

regexp = re.compile(sys.argv[1])

for line in sys.stdin:
	line = line.rstrip("\r\n")
	if regexp.search(line):
		print line
