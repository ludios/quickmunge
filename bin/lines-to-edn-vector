#!/usr/bin/python

"""
Put all lines in stdin into an edn vector
"""

import sys
# https://github.com/swaroopch/edn_format
import edn_format

arr = []
for line in sys.stdin:
	line = line.rstrip("\r\n")
	arr.append(line)
print edn_format.dumps(arr)
